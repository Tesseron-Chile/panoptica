Done. Here's what was delivered:

**New test files (66 total tests, all green):**
- `CampusView.test.tsx` — 4 tests: placeholder when empty, N cards for N runs, HotDeskArea session filtering, run card click calls `goToRunOffice`
- `RunOfficeView.test.tsx` — 4 tests: Orchestrator + 4 RoleNooks, active/inactive opacity, back button calls `goToCampus`, active nook click calls `goToNook`
- `TaskWhiteboard.test.tsx` — 5 tests: column headers, empty placeholders, task grouping by status, correct counts

**Config changes:**
- `vitest.config.ts` — added `environment: 'happy-dom'`, CSS mock Vite plugin, `setupFiles`
- `vitest.setup.ts` — wires `jest-dom` matchers + explicit RTL `cleanup` per test

PR #5 render-test comment replied with fix summary. Committed and pushed as `ce57f71`.
